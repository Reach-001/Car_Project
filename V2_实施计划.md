# V2 框架重构实施计划

> 原则：渐进式重构，新建并行目录，每阶段可编译验证，最后一次性切换 App 层。
> BSP 抽象级别：头文件不暴露 HAL 类型，实现层直接用 HAL 函数。

---

## 目录总览

### V2 目标结构

```
Car_Project/
├── App/
│   ├── Inc/
│   │   ├── app_main.h          # [不变] 应用入口声明
│   │   ├── app_tasks.h         # [不变] 任务注册声明
│   │   └── system_state_pool.h # [新增] 系统状态池 —— 唯一跨域数据交换中心
│   └── Src/
│       ├── app_main.c          # [Phase6 重构] 改为调用 V2 模块
│       ├── app_tasks.c         # [Phase6 重构] 改为 V2 任务编排
│       └── system_state_pool.c # [新增] 状态池实现
│
├── Service/
│   ├── Inc/
│   │   ├── scheduler.h         # [不变] 调度器
│   │   └── ring_buffer.h       # [新增] ISR 安全的环形缓冲区
│   └── Src/
│       ├── scheduler.c         # [不变]
│       └── ring_buffer.c       # [新增]
│
├── BSP/
│   ├── Actuator/               # [重组] 执行器
│   │   ├── Inc/bsp_motor.h     # [搬移] 接口不变，确保不暴露 HAL 类型
│   │   ├── Src/bsp_motor.c     # [搬移] 实现不变
│   │   ├── Inc/bsp_servo.h     # [搬移] 接口不变
│   │   └── Src/bsp_servo.c     # [搬移] 实现不变
│   │
│   ├── Sensor/                 # [重组] 传感器硬件抽象
│   │   ├── Inc/bsp_encoder.h   # [搬移] 接口不变
│   │   ├── Src/bsp_encoder.c   # [搬移] 实现不变
│   │   ├── Inc/bsp_gpio_sensor.h # [新建] 循迹 GPIO + 超声波 GPIO
│   │   └── Src/bsp_gpio_sensor.c # [新建] 从 bsp_io 拆出传感器部分
│   │
│   ├── HMI/                    # [重组] 人机交互硬件抽象
│   │   ├── Inc/bsp_key.h       # [搬移] 接口不变
│   │   ├── Src/bsp_key.c       # [搬移] 实现不变
│   │   ├── Inc/bsp_buzzer.h    # [搬移] 接口不变
│   │   ├── Src/bsp_buzzer.c    # [搬移] 实现不变
│   │   ├── Inc/bsp_led.h       # [新建] 从 bsp_io 拆出 LED
│   │   └── Src/bsp_led.c       # [新建]
│   │
│   └── Comm/                   # [新建] 通信硬件抽象
│       ├── Inc/bsp_uart.h      # [新建] UART + ring buffer 绑定
│       └── Src/bsp_uart.c      # [新建]
│
├── Modules/
│   ├── Sensor/                 # [新建] 传感器域
│   │   ├── Inc/sensor_domain.h
│   │   └── Src/
│   │       ├── sensor_domain.c  # 域聚合入口
│   │       ├── tracker_sensor.c # [迁移自 tracker.c]
│   │       └── ultrasonic_sensor.c # [迁移自 ultrasonic.c]
│   │
│   ├── Estimation/             # [新建] 速度估计域
│   │   ├── Inc/estimation.h
│   │   └── Src/estimation.c
│   │
│   ├── Motion/                 # [新建] 运动控制域
│   │   ├── Inc/motion.h
│   │   └── Src/
│   │       ├── motion.c        # 域聚合入口
│   │       ├── ackermann.c     # 阿克曼几何分配
│   │       └── speed_pi.c      # 左右轮速度 PI
│   │
│   ├── Decision/               # [新建] 决策域
│   │   ├── Inc/decision.h
│   │   └── Src/decision.c      # [迁移自 app_mode.c，去除硬件依赖]
│   │
│   ├── Comm/                   # [新建] 通信域
│   │   ├── Inc/comm.h
│   │   └── Src/
│   │       ├── comm.c          # 域聚合入口
│   │       ├── bt_link.c       # [迁移] 蓝牙协议解析
│   │       ├── k230_link.c     # [迁移] K230 协议解析
│   │       └── protocol.c      # [搬移] 协议行解析器
│   │
│   └── HMI/                    # [新建] 人机交互域
│       ├── Inc/hmi.h
│       └── Src/
│           ├── hmi.c           # 域聚合入口
│           ├── key_service.c   # 按键事件 → 状态池
│           ├── buzzer_service.c # 状态池模式 → 提示音
│           └── led_service.c   # 状态池模式 → LED
```

### 旧文件处理策略

所有旧文件保留不动，直到 Phase 6 一次性删除。旧文件列表：

| 旧文件 | 处理 |
|---|---|
| `BSP/Inc/bsp_io.h` `BSP/Src/bsp_io.c` | Phase 6 删除，功能拆分到 bsp_led / bsp_gpio_sensor |
| `BSP/Inc/bsp_motor.h` `BSP/Src/bsp_motor.c` | Phase 6 删除，已搬移到 BSP/Actuator |
| `BSP/Inc/bsp_servo.h` `BSP/Src/bsp_servo.c` | Phase 6 删除，已搬移到 BSP/Actuator |
| `BSP/Inc/bsp_encoder.h` `BSP/Src/bsp_encoder.c` | Phase 6 删除，已搬移到 BSP/Sensor |
| `BSP/Inc/bsp_key.h` `BSP/Src/bsp_key.c` | Phase 6 删除，已搬移到 BSP/HMI |
| `BSP/Inc/bsp_buzzer.h` `BSP/Src/bsp_buzzer.c` | Phase 6 删除，已搬移到 BSP/HMI |
| `Modules/Inc/chassis.h` `Modules/Src/chassis.c` | Phase 6 删除，拆分为 Estimation + Motion |
| `Modules/Inc/app_mode.h` `Modules/Src/app_mode.c` | Phase 6 删除，迁移到 Decision |
| `Modules/Inc/tracker.h` `Modules/Src/tracker.c` | Phase 6 删除，迁移到 Sensor 域 |
| `Modules/Inc/ultrasonic.h` `Modules/Src/ultrasonic.c` | Phase 6 删除，迁移到 Sensor 域 |
| `Modules/Inc/bt_link.h` `Modules/Src/bt_link.c` | Phase 6 删除，迁移到 Comm 域 |
| `Modules/Inc/k230_link.h` `Modules/Src/k230_link.c` | Phase 6 删除，迁移到 Comm 域 |
| `Modules/Inc/protocol.h` `Modules/Src/protocol.c` | Phase 6 删除，搬移到 Comm 域 |

---

## 分层接口规范（所有模块必须遵守）

每个模块的 `.h` 文件只暴露四类接口：

```c
/* 1. 初始化 —— 只调用一次，在 App_Init() 中 */
void <Module>_Init(void);

/* 2. 周期任务 —— 被调度器调用，入参为 SystemStatePool 指针 */
void <Module>_TaskXms(SystemStatePool *pool);

/* 3. 状态获取 —— 只读，返回模块内部状态快照，仅供调试/监控 */
<Module>State <Module>_GetState(void);

/* 4. 命令设置 —— 外部（App/测试）向模块注入命令，非周期调用 */
void <Module>_SetCommand(const <Module>Command *cmd);
```

模块内部规则：
- 所有状态变量、PID 参数、滤波队列、状态机变量、解析缓存 必须是 `static`
- 模块 `.h` 不暴露内部结构体实现
- 模块不得 `#include` 其他模块的头文件（只能 include BSP 和 Service）

---

## Phase 0：基础设施（0 依赖，不触动任何旧代码）

### 目标
创建 RingBuffer 和 SystemStatePool，这两个是后续所有模块的公共依赖。

### 文件清单

#### 0.1 `Service/Inc/ring_buffer.h`

```c
// 单个 ring buffer 实例，ISR 安全（单生产者单消费者无锁）
// 用于 UART 接收：ISR 写，Task 读

typedef struct {
    uint8_t *buffer;
    uint16_t capacity;
    volatile uint16_t head;  // ISR 写
    uint16_t tail;            // Task 读
} RingBuffer;

void RingBuffer_Init(RingBuffer *rb, uint8_t *storage, uint16_t capacity);
bool RingBuffer_PushFromIsr(RingBuffer *rb, uint8_t byte);
bool RingBuffer_Pop(RingBuffer *rb, uint8_t *byte);
uint16_t RingBuffer_Available(const RingBuffer *rb);
void RingBuffer_Flush(RingBuffer *rb);
```

#### 0.2 `App/Inc/system_state_pool.h`

```c
// 系统状态池 —— 唯一跨域数据交换中心
// 位于 App 层，所有域通过 pool 指针读写数据
// 域之间不直接调用对方函数

typedef enum {
    SYS_MODE_STOP = 0,
    SYS_MODE_MANUAL,
    SYS_MODE_LINE_FOLLOW,
    SYS_MODE_INSPECTION,
    SYS_MODE_AVOIDANCE,
    SYS_MODE_ERROR
} SystemMode;

// --- 各域写入区域 ---

// 目标值（Decision 写入，Motion 读取）
typedef struct {
    float speed_mps;
    float steer_angle_rad;
    bool valid;
} SystemTarget;

// 速度估计（Estimation 写入，Motion/Decision 读取）
typedef struct {
    float left_speed_mps;
    float right_speed_mps;
    float body_speed_mps;
    int32_t left_encoder_delta;
    int32_t right_encoder_delta;
    bool valid;
} SystemEstimation;

// 传感器物理量（Sensor 写入，Decision 读取）
typedef struct {
    uint8_t track_bits;
    int16_t track_error;
    bool track_valid;
    uint16_t ultrasonic_mm;
    bool ultrasonic_valid;
    bool obstacle_near;
} SystemSensor;

// 一次性事件标志（Comm/HMI 写入，Decision 读取并清除）
typedef struct {
    bool bt_command_ready;
    bool k230_result_ready;
    bool key_stop_clicked;
    bool key_mode_clicked;
    bool task_start_requested;
    bool task_pause_requested;
} SystemEvent;

// 故障标志（各域均可写入，Decision 读取仲裁）
typedef struct {
    bool heartbeat_lost;
    bool obstacle_too_close;
    bool sensor_invalid;
    bool motor_stall;
    bool servo_limit;
    bool emergency_stop;
} SystemFault;

// 通信数据暂存（Comm 写入，Decision 读取）
typedef struct {
    BtCommand bt_command;
    K230Result k230_result;
} CommData;

// 顶层状态池
typedef struct {
    SystemMode mode;
    SystemTarget target;
    SystemEstimation estimation;
    SystemSensor sensor;
    SystemEvent event;
    SystemFault fault;
    CommData comm;
    uint32_t tick_ms;   // 由 App 在每轮调度前更新
} SystemStatePool;

// 接口
void SystemStatePool_Init(SystemStatePool *pool);
void SystemStatePool_ClearCycleEvents(SystemStatePool *pool);  // 每轮 Decision 后清除一次性事件
```

> 设计要点：
> - `pool` 是裸机单线程共享数据，不需要锁
> - 每个域的 `_TaskXms()` 只读写自己负责的字段
> - `event` 是一次性的，`Decision_Task20ms` 消费后调用 `ClearCycleEvents` 清除
> - `fault` 是持久的，由各域置位，Decision 决定如何处理

### 编译验证
添加 2 个新 .c 文件到 CMakeLists.txt，编译通过即可。旧代码完全不受影响。

---

## Phase 1：BSP 层重组（从下往上，先硬件后逻辑）

### 目标
按硬件类别重组 BSP，每个子目录对应一类外设。头文件不暴露 HAL 类型。建立 ISR → RingBuffer 的标准模式。

### 1.1 BSP/Actuator（搬移，无逻辑变化）

**`BSP/Actuator/Inc/bsp_motor.h`** — 从 `BSP/Inc/bsp_motor.h` 搬移，接口完全相同。

**`BSP/Actuator/Src/bsp_motor.c`** — 从 `BSP/Src/bsp_motor.c` 搬移，调整 include 路径。

**`BSP/Actuator/Inc/bsp_servo.h`** — 搬移，接口不变。额外增加：
```c
void BspServo_SetPulseUs(uint16_t pulse_us);  // 直接设脉宽，Motion 用
```

**`BSP/Actuator/Src/bsp_servo.c`** — 搬移，新增 `SetPulseUs` 实现（含 1000us/2000us 硬限幅）。

### 1.2 BSP/Sensor（搬移 + 新建）

**`BSP/Sensor/Inc/bsp_encoder.h`** — 搬移，接口不变。

**`BSP/Sensor/Src/bsp_encoder.c`** — 搬移，调整 include 路径。

**`BSP/Sensor/Inc/bsp_gpio_sensor.h`** — 新建，从 bsp_io 拆出：
```c
// 循迹 GPIO 读取（5 路黑线传感器）
typedef struct {
    uint8_t bits;
    bool sensor[5];
} BspTrackRaw;

BspTrackRaw BspGpioSensor_ReadTrack(void);

// 超声波 GPIO 控制
void BspGpioSensor_TrigSet(bool high);
bool BspGpioSensor_EchoRead(void);
```

**`BSP/Sensor/Src/bsp_gpio_sensor.c`** — 新建，实现上述函数（从 bsp_io.c 搬逻辑）。

### 1.3 BSP/HMI（搬移 + 新建）

**`BSP/HMI/Inc/bsp_key.h`** — 搬移，接口不变。

**`BSP/HMI/Src/bsp_key.c`** — 搬移，确保不依赖外部（当前实现已是独立的，只用了 HAL）。

**`BSP/HMI/Inc/bsp_buzzer.h`** — 搬移，接口不变。

**`BSP/HMI/Src/bsp_buzzer.c`** — 搬移，调整 include 路径。

**`BSP/HMI/Inc/bsp_led.h`** — 新建：
```c
void BspLed_Init(void);
void BspLed_SetStateLed(bool on);
```

**`BSP/HMI/Src/bsp_led.c`** — 新建，从 bsp_io.c 搬 LED 逻辑。

### 1.4 BSP/Comm（新建）

**`BSP/Comm/Inc/bsp_uart.h`** — 新建：
```c
// 将 UART 外设与 RingBuffer 绑定
// ISR 中只做 PushFromIsr，Task 中做 Pop 和解析

typedef enum {
    BSP_UART_K230 = 0,   // USART2
    BSP_UART_BT           // USART3
} BspUartId;

void BspUart_Init(BspUartId id);
bool BspUart_ReadByte(BspUartId id, uint8_t *byte);  // 非阻塞读一个字节
void BspUart_WriteByte(BspUartId id, uint8_t byte);  // 阻塞写
void BspUart_WriteString(BspUartId id, const char *str);
void BspUart_IrqHandler(BspUartId id);  // 在 USARTx_IRQHandler 中调用
```

> 注意：这个模块内部持有 RingBuffer 实例和 static buffer。不暴露 RingBuffer 细节出去。

**`BSP/Comm/Src/bsp_uart.c`** — 新建，实现。

### 编译验证
添加 BSP/Actuator, BSP/Sensor, BSP/HMI, BSP/Comm 的新 .c 文件到 CMakeLists.txt，编译通过。旧 BSP 文件仍然在编译中（被旧模块引用），新旧 BSP 共存。

---

## Phase 2：Sensor 域（读取物理传感器 → 写入 pool->sensor）

### 目标
将循迹和超声波传感器封装为一个 Sensor 域。内部可以有多个子文件，但对外只有 `sensor_domain.h` 暴露接口。

### 接口

**`Modules/Sensor/Inc/sensor_domain.h`**
```c
typedef struct {
    uint8_t track_bits;
    int16_t track_error;
    bool track_valid;
    uint16_t ultrasonic_mm;
    bool ultrasonic_valid;
    bool obstacle_near;
} SensorState;   // 与 SystemSensor 结构一致，方便赋值

void Sensor_Init(void);
void Sensor_Task20ms(SystemStatePool *pool);
SensorState Sensor_GetState(void);
```

### 内部实现

- `sensor_domain.c` — 聚合入口，调用子模块，写入 `pool->sensor.*`
- `tracker_sensor.c` — 从旧 `tracker.c` 迁移，内部 static 状态，对外只通过 `sensor_domain.c` 的聚合函数暴露
- `ultrasonic_sensor.c` — 从旧 `ultrasonic.c` 迁移，同上

### 依赖
只依赖：
- `BSP/Sensor/bsp_gpio_sensor.h`
- `App/system_state_pool.h`

不依赖任何其他 Module。

### 接口契约（关键）

```
输入：BSP GPIO 读取
输出：pool->sensor.track_bits / track_error / track_valid
      pool->sensor.ultrasonic_mm / ultrasonic_valid
      pool->sensor.obstacle_near
      pool->fault.sensor_invalid（传感器故障时置位）
禁止：调用任何其他模块函数、直接操作 PWM/舵机
```

---

## Phase 3：Comm 域（解析通信协议 → 写入 pool->comm / pool->event）

### 目标
封装蓝牙和 K230 的通信，只解析协议，只产出数据，不执行任何底盘动作。

### 接口

**`Modules/Comm/Inc/comm.h`**
```c
typedef struct {
    BtCommand bt_command;
    K230Result k230_result;
    bool bt_connected;
    bool k230_connected;
    uint32_t bt_last_rx_ms;
    uint32_t k230_last_rx_ms;
} CommState;

void Comm_Init(void);
void Comm_Task20ms(SystemStatePool *pool);
CommState Comm_GetState(void);
void Comm_SetCommand(const CommCommand *cmd);  // 预留，用于向外部设备发送数据
```

### 内部实现

- `comm.c` — 聚合入口，调用 bt_link 和 k230_link，将解析结果写入 `pool->comm.*` 和 `pool->event.*`
- `bt_link.c` — 从旧 `bt_link.c` 迁移，清除掉旧版中直接调 AppMode 的逻辑
- `k230_link.c` — 从旧 `k230_link.c` 迁移，同上
- `protocol.c` — 从旧搬移，无逻辑变化

### 依赖
只依赖：
- `BSP/Comm/bsp_uart.h`
- `App/system_state_pool.h`

### 接口契约

```
输入：UART ring buffer 字节流
输出：pool->comm.bt_command / k230_result
      pool->event.bt_command_ready / k230_result_ready
      pool->fault.heartbeat_lost（通信超时）
禁止：#include "chassis.h" "#include "motion.h" 等任何控制相关头文件
禁止：调用任何 BSP Actuator 函数
```

---

## Phase 4：Estimation 域（编码器计数 → 车速 m/s）

### 目标
从编码器脉冲增量计算出左右轮速度（m/s）和车身速度。所有标定参数（轮径、减速比、编码器线数）作为内部 static 常量。

### 接口

**`Modules/Estimation/Inc/estimation.h`**
```c
typedef struct {
    float left_speed_mps;
    float right_speed_mps;
    float body_speed_mps;
    int32_t left_encoder_delta;
    int32_t right_encoder_delta;
    bool valid;
} EstimationState;

void Estimation_Init(void);
void Estimation_Task10ms(SystemStatePool *pool);
EstimationState Estimation_GetState(void);
```

### 内部 static 参数

```c
static const float WHEEL_DIAMETER_M      = 0.065f;   // 待实车标定
static const float ENCODER_PPR           = 13.0f;    // 编码器线数
static const float GEAR_RATIO            = 30.0f;    // 减速比
static const int8_t LEFT_DIR             = 1;        // 方向因子，+1 或 -1
static const int8_t RIGHT_DIR            = 1;
```

### 依赖
只依赖：
- `BSP/Sensor/bsp_encoder.h`
- `App/system_state_pool.h`

### 接口契约

```
输入：编码器增量计数（来自 BSP）
输出：pool->estimation.left_speed_mps / right_speed_mps / body_speed_mps
      pool->estimation.left_encoder_delta / right_encoder_delta（原始值供调试）
      pool->estimation.valid
禁止：调用任何控制函数
```

---

## Phase 5：Motion 域（目标速度/转角 → PWM/舵机输出）

### 目标
读取 `pool->target`，执行阿克曼几何分配 + 速度 PI 闭环，输出到 BSP Actuator。这是唯一可以操作 PWM 和舵机的模块域。

### 接口

**`Modules/Motion/Inc/motion.h`**
```c
typedef struct {
    float target_speed_mps;
    float target_steer_rad;
    float left_target_mps;
    float right_target_mps;
    int16_t left_pwm_permille;
    int16_t right_pwm_permille;
    uint16_t servo_pulse_us;
    bool limited;       // 输出达到限幅边界
} MotionState;

void Motion_Init(void);
void Motion_Task10ms(SystemStatePool *pool);
MotionState Motion_GetState(void);
void Motion_SetCommand(const MotionCommand *cmd);  // 紧急刹车等外部命令
```

### 内部实现

- `motion.c` — 聚合入口：读取 pool->target + pool->estimation，输出 PWM/舵机，写入 pool->fault
- `ackermann.c` — 阿克曼几何：
  ```
  输入：body_speed_mps, steer_angle_rad, 轴距 wheelbase_m, 轮距 track_m
  输出：left_target_mps, right_target_mps
  ```
- `speed_pi.c` — 左右轮独立 PI：
  ```
  输入：target_mps, actual_mps (from pool->estimation)
  输出：pwm_permille (-1000..1000)
  ```

### 依赖
只依赖：
- `BSP/Actuator/bsp_motor.h`
- `BSP/Actuator/bsp_servo.h`
- `App/system_state_pool.h`

### 接口契约（关键——Motion 的安全职责）

```
输入：pool->target.speed_mps / steer_angle_rad
      pool->estimation.left_speed_mps / right_speed_mps（PI 反馈）
输出：BSP Motor PWM, BSP Servo pulse
      pool->fault.motor_stall

安全责任：
  1. speed_mps 按实车能力限幅
  2. steer_angle_rad 按机械结构限幅
  3. 左右轮 PWM 限制在 -1000..1000
  4. 舵机脉宽限制在 1000us..2000us（即使 BSP 有硬限幅，这里也做一遍）
  5. 检测堵转（target>0 但编码器 delta==0 持续一段时间）

禁止：
  - 解析任何通信协议
  - 读取按键
  - 读取传感器（只读 estimation 和 target）
  - 调用 Decision
```

---

## Phase 6：Decision 域（事件仲裁 + 模式管理）

### 目标
这是整个系统的 "大脑"。读取所有输入（event, fault, sensor, comm, estimation），仲裁模式，计算目标速度和转角。**不操作任何硬件**。

### 接口

**`Modules/Decision/Inc/decision.h`**
```c
typedef struct {
    SystemMode mode;
    SystemMode prev_mode;
    uint32_t mode_enter_ms;
    float target_speed_mps;
    float target_steer_rad;
} DecisionState;

void Decision_Init(void);
void Decision_Task20ms(SystemStatePool *pool);
DecisionState Decision_GetState(void);
void Decision_SetCommand(const DecisionCommand *cmd);
```

### 内部状态机

```
事件仲裁优先级（从高到低）：

1. pool->fault.emergency_stop    → SYS_MODE_STOP  (target = 0)
2. pool->fault.heartbeat_lost   → SYS_MODE_STOP  (target = 0)
3. pool->fault.obstacle_too_close → SYS_MODE_AVOIDANCE
4. pool->event.bt_command_ready
   - BT_COMMAND_STOP        → SYS_MODE_STOP
   - BT_COMMAND_MANUAL_MOVE → SYS_MODE_MANUAL (arg0=speed_permille, arg1=steer_permille)
   - BT_COMMAND_START_TASK  → SYS_MODE_INSPECTION
   - BT_COMMAND_SET_MODE    → 切换模式
5. pool->event.key_stop_clicked  → SYS_MODE_STOP
6. pool->event.key_mode_clicked  → 模式轮转
7. 当前模式的持续控制逻辑：
   - SYS_MODE_STOP:        target=(0,0)
   - SYS_MODE_MANUAL:      target=蓝牙命令转换
   - SYS_MODE_LINE_FOLLOW: target=循迹控制算法（从 sensor.track_error 计算）
   - SYS_MODE_INSPECTION:  target=巡检状态机
   - SYS_MODE_AVOIDANCE:   target=避障策略
   - SYS_MODE_ERROR:       target=(0,0) + 蜂鸣器报警

输出：
  pool->mode = 当前模式
  pool->target.speed_mps = 目标速度
  pool->target.steer_angle_rad = 目标转角
  pool->target.valid = true
```

### 依赖
只依赖：
- `App/system_state_pool.h`

不依赖任何 BSP 或 Module 头文件。

### 接口契约（关键）

```
输入：pool->event / fault / sensor / comm / estimation / mode
输出：pool->mode / target.speed_mps / target.steer_angle_rad / target.valid
      调用 SystemStatePool_ClearCycleEvents(pool) 清理一次性事件

禁止：
  #include "chassis.h" / "motion.h" / "bsp_motor.h" / "bsp_servo.h"
  任何 PWM 操作、舵机操作
```

---

## Phase 7：HMI 域（按键/蜂鸣器/LED → 写入事件 + 响应状态）

### 目标
封装人机交互。按键事件写入 pool->event，根据 pool->mode 和 pool->fault 更新蜂鸣器和 LED。

### 接口

**`Modules/HMI/Inc/hmi.h`**
```c
typedef struct {
    bool led_on;
    BuzzerPattern active_pattern;
    bool key1_pressed;
    bool key2_pressed;
    bool key3_pressed;
} HmiState;

void Hmi_Init(void);
void Hmi_Task500ms(SystemStatePool *pool);
HmiState Hmi_GetState(void);
void Hmi_SetCommand(const HmiCommand *cmd);
```

### 内部实现

- `hmi.c` — 聚合入口
- `key_service.c` — 读取 BspKey 事件，写入 `pool->event.key_stop_clicked` 等
- `buzzer_service.c` — 根据 `pool->mode` 和 `pool->fault` 选择提示音模式
- `led_service.c` — 根据 `pool->mode` 设置 LED 闪烁策略

### 依赖
只依赖：
- `BSP/HMI/bsp_key.h`
- `BSP/HMI/bsp_buzzer.h`
- `BSP/HMI/bsp_led.h`
- `App/system_state_pool.h`

### 接口契约

```
输入：BSP Key 事件, pool->mode, pool->fault
输出：pool->event.key_stop_clicked / key_mode_clicked
      蜂鸣器提示音（通过 BSP）
      LED 状态（通过 BSP）

禁止：
  调用 Motion / Chassis 停止函数
  直接操作 PWM / 舵机
```

---

## Phase 8：App 层切换（删除旧代码，接入 V2）

### App 层的职责

App 层是唯一的"胶水层"。它做三件事：
1. `App_Init()` — 按依赖顺序初始化所有模块
2. `AppTasks_Register()` — 按时间槽注册任务
3. `App_Run()` — 调用调度器

### `app_main.c` V2 版本

```c
#include "app_main.h"
#include "app_tasks.h"
#include "system_state_pool.h"
#include "scheduler.h"

// BSP
#include "bsp_motor.h"       // → BSP/Actuator
#include "bsp_servo.h"
#include "bsp_encoder.h"     // → BSP/Sensor
#include "bsp_gpio_sensor.h"
#include "bsp_key.h"         // → BSP/HMI
#include "bsp_buzzer.h"
#include "bsp_led.h"
#include "bsp_uart.h"        // → BSP/Comm

// Modules
#include "sensor_domain.h"
#include "estimation.h"
#include "motion.h"
#include "decision.h"
#include "comm.h"
#include "hmi.h"

static SystemStatePool g_state;

void App_Init(void)
{
    // 1. BSP 初始化（硬件层）
    BspLed_Init();
    BspBuzzer_Init();
    BspKey_Init();
    BspMotor_Init();
    BspEncoder_Init();
    (void)BspServo_Init();
    BspUart_Init(BSP_UART_K230);
    BspUart_Init(BSP_UART_BT);

    // 2. 状态池初始化
    SystemStatePool_Init(&g_state);

    // 3. 模块域初始化（逻辑层，按依赖顺序）
    Sensor_Init();
    Estimation_Init();
    Motion_Init();
    Comm_Init();
    Decision_Init();
    Hmi_Init();

    // 4. 调度器
    Scheduler_Init();
    AppTasks_Register();
}

void App_Run(void)
{
    g_state.tick_ms = HAL_GetTick();
    Scheduler_Run(g_state.tick_ms);
}
```

### `app_tasks.c` V2 版本

```c
#include "app_tasks.h"
#include "system_state_pool.h"
#include "scheduler.h"
#include "sensor_domain.h"
#include "estimation.h"
#include "motion.h"
#include "decision.h"
#include "comm.h"
#include "hmi.h"

// 全局状态池（由 app_main.c 定义）
extern SystemStatePool g_state;

// --- 10ms 硬实时槽 ---

static void Task_Estimation10ms(void *ctx) {
    Estimation_Task10ms((SystemStatePool *)ctx);
}

static void Task_Motion10ms(void *ctx) {
    Motion_Task10ms((SystemStatePool *)ctx);
}

// --- 20ms 逻辑槽 ---

static void Task_Sensor20ms(void *ctx) {
    Sensor_Task20ms((SystemStatePool *)ctx);
}

static void Task_Comm20ms(void *ctx) {
    Comm_Task20ms((SystemStatePool *)ctx);
}

static void Task_Decision20ms(void *ctx) {
    Decision_Task20ms((SystemStatePool *)ctx);
    SystemStatePool_ClearCycleEvents((SystemStatePool *)ctx);
}

// --- 500ms 低优先槽 ---

static void Task_Hmi500ms(void *ctx) {
    Hmi_Task500ms((SystemStatePool *)ctx);
}

void AppTasks_Register(void)
{
    // 10ms: 先估计速度，再执行控制
    Scheduler_AddTask("estimation", Task_Estimation10ms, &g_state, 10U, 0U);
    Scheduler_AddTask("motion",     Task_Motion10ms,     &g_state, 10U, 1U);

    // 20ms: 传感器 → 通信 → 决策（故意让决策最后，消费本轮所有事件）
    Scheduler_AddTask("sensor",     Task_Sensor20ms,     &g_state, 20U, 2U);
    Scheduler_AddTask("comm",       Task_Comm20ms,       &g_state, 20U, 4U);
    Scheduler_AddTask("decision",   Task_Decision20ms,   &g_state, 20U, 6U);

    // 500ms: HMI 最慢，不影响控制
    Scheduler_AddTask("hmi",        Task_Hmi500ms,       &g_state, 500U, 0U);
}
```

### 更新 CMakeLists.txt

删除旧文件引用，添加所有 V2 新文件。更新 include 路径。

### 删除旧文件

删除 Phase 0 文档中列出的所有旧文件。

---

## 时间槽设计总结

| 槽位 | 周期 | 任务 | 顺序理由 |
|---:|---:|---|---|
| estimation | 10ms | 编码器 → 速度估计 | 必须先于 Motion，提供反馈 |
| motion | 10ms | 读取 target → PI → 写 PWM | 紧随 Estimation，使用最新反馈 |
| sensor | 20ms | 循迹 + 超声波更新 | 独立，不依赖其他域 |
| comm | 20ms | 蓝牙 + K230 协议解析 | 独立，产出事件 |
| decision | 20ms | 事件仲裁 + 模式控制 | 最后执行，消费本轮所有事件后清除 |
| hmi | 500ms | 按键事件 + LED + 蜂鸣器 | 慢速，不影响控制实时性 |

---

## 数据流全貌（以一次蓝牙 STOP 为例）

```
BtLink 在 USART3 ISR 收到字节
  → RingBuffer_PushFromIsr()

Comm_Task20ms:
  → BspUart_ReadByte() 循环取字节
  → 协议解析出 BT_COMMAND_STOP
  → 写入 pool->comm.bt_command
  → 写入 pool->event.bt_command_ready = true

Decision_Task20ms (20ms 槽最后一个):
  → 读取 pool->event.bt_command_ready == true
  → 仲裁：STOP 优先级 > 当前模式
  → 设置 pool->mode = SYS_MODE_STOP
  → 设置 pool->target = { speed_mps: 0, steer_angle_rad: 0, valid: true }
  → SystemStatePool_ClearCycleEvents() 清除 bt_command_ready

Motion_Task10ms (下一轮 10ms):
  → 读取 pool->target = (0, 0)
  → 阿克曼分配：左右轮目标 = 0
  → 速度 PI：error = 0 - actual, 计算刹车 PWM
  → BspMotor_SetDuty() / BspServo_SetPulseUs(1500)
  → 车身停下来
```

注意整个过程没有任何模块直接调用另一个模块的函数。所有跨域交互都通过 SystemStatePool 完成。

---

## 每个 Phase 的编译验证步骤

| Phase | 新增文件 | CMake 改动 | 验证方式 |
|---|---|---|---|
| 0 | ring_buffer.c, system_state_pool.c | 添加 2 个 .c | 编译通过 |
| 1 | BSP/Actuator/*, BSP/Sensor/*, BSP/HMI/*, BSP/Comm/* | 添加所有新 BSP .c | 编译通过（旧 BSP 仍在） |
| 2 | Modules/Sensor/* | 添加 sensor .c | 编译通过 |
| 3 | Modules/Comm/* | 添加 comm .c | 编译通过 |
| 4 | Modules/Estimation/* | 添加 estimation .c | 编译通过 |
| 5 | Modules/Motion/* | 添加 motion .c | 编译通过 |
| 6 | Modules/Decision/* | 添加 decision .c | 编译通过 |
| 7 | Modules/HMI/* | 添加 hmi .c | 编译通过 |
| 8 | app_main.c, app_tasks.c 重写 | 删除旧文件引用，添加全部 V2 文件 | **关键验证点**：编译通过 + 实车测试 |

---

## 移植性保障措施

1. **BSP 是唯一的 HAL 接触点**：上层 Module 和 App 层不 include 任何 HAL 头文件
2. **SystemStatePool 无平台依赖**：只有 `stdint.h`、`stdbool.h`
3. **Scheduler 无平台依赖**：只依赖 `stdint.h`、`stdbool.h`，时间源由外部传入
4. **RingBuffer 无平台依赖**：纯 C 标准库
5. **换芯片时**：只需重写 `BSP/` 下的 .c 文件，Module 层代码一行不改

---

## 调试便利性设计

虽然你说暂时不加调试系统，但架构上已经预留了：
1. 每个域都有 `GetState()` 返回完整内部状态快照
2. `SystemStatePool` 在 App 层是全局的，调试时可在任意位置 dump 整个状态
3. 所有模块内部状态 `static`，外部只能通过 `GetState()` 观察，不会被意外篡改
4. 函数调用链单向：App → Domain → BSP，不会出现循环调用

---

## 下一步

确认计划后，我从 **Phase 0** 开始，按顺序逐个 Phase 实施。每完成一个 Phase 编译验证后继续下一个。最终 Phase 8 一次性切换 App 层并删除旧代码。

预计总共新建约 25 个文件，修改 2 个文件（CMakeLists.txt + App 层），删除约 18 个旧文件。
