# V3 速度环实车标定

## 测试入口

速度环 PID 标定是前期 Debug 测试项，不写入主任务逻辑。

使用方式：

```c
#define TEST_SELECT  TEST_SPEED_PID
```

重新编译烧录后，系统只运行：

```text
BSP 按键/电机/编码器/UART → Estimation → Motion → Debug 曲线输出
```

不经过 `Comm / Decision / AppTasks` 主业务框架。

## 按键控制

| 按键 | 功能 |
|---|---|
| KEY1 | 速度目标清零，停车/刹车 |
| KEY2 | 速度目标减小一档，最低到 0，不进入倒车 |
| KEY3 | 速度目标增加一档 |
| KEY4 / User_Key | 转向角回中 |

步长由 `VEHICLE_DEBUG_SPEED_STEP_MPS` 配置，默认 `0.10 m/s`。

## 曲线通道

`TEST_SPEED_PID` 会默认开启 Debug 曲线输出，蓝牙输出 10 通道 little-endian float 帧，周期 100ms。

| 通道 | 含义 |
|---|---|
| CH1 | 系统模式 |
| CH2 | 车身目标速度 `target.speed_mps` |
| CH3 | 车身实际速度 `estimation.body_speed_mps` |
| CH4 | 左轮目标速度 `motion.left_target_mps` |
| CH5 | 左轮实际速度 `estimation.left_speed_mps` |
| CH6 | 左轮 PWM `motion.left_pwm` |
| CH7 | 右轮目标速度 `motion.right_target_mps` |
| CH8 | 右轮实际速度 `estimation.right_speed_mps` |
| CH9 | 右轮 PWM `motion.right_pwm` |
| CH10 | 故障/限幅 bitmask |

## 参数位置

所有实车标定优先修改：

```text
App/Inc/vehicle_config.h
```

关键参数：

```c
VEHICLE_WHEEL_DIAMETER_M
VEHICLE_ENCODER_PPR
VEHICLE_GEAR_RATIO
VEHICLE_LEFT_ENCODER_SIGN
VEHICLE_RIGHT_ENCODER_SIGN

VEHICLE_SPEED_PI_KP
VEHICLE_SPEED_PI_KI
VEHICLE_SPEED_PI_I_ENABLE
VEHICLE_SPEED_PI_I_MAX
VEHICLE_SPEED_PI_TARGET_DEADBAND
VEHICLE_SPEED_PI_FF_MIN_PWM
VEHICLE_SPEED_PI_FF_GAIN_PWM_PER_MPS
```

当前电机配置：

- 电机：310 减速电机
- 减速比：`1:20`
- 编码器读取的是电机端转速，实际轮端输出速度需要再除以减速比 `20`。

## 标定步骤

### 1. 编码器方向确认

发送：

```text
KEY3 按两次，让目标速度到 0.20 m/s
```

观察：

- CH4/CH7 目标速度为正。
- CH5/CH8 实际速度也应为正。

如果某一侧前进时实际速度为负，修改：

```c
VEHICLE_LEFT_ENCODER_SIGN
VEHICLE_RIGHT_ENCODER_SIGN
```

### 2. 前馈最低 PWM 标定

先保持 I 项关闭：

```c
#define VEHICLE_SPEED_PI_I_ENABLE 0
```

从低速开始：

```text
KEY3 每次增加 0.10 m/s
KEY1 随时清零停车
```

观察 CH6/CH9 PWM 和 CH5/CH8 实际速度。

调整：

- 车轮不动或偶尔停：增大 `VEHICLE_SPEED_PI_FF_MIN_PWM`
- 一给很小速度就冲得太快：减小 `VEHICLE_SPEED_PI_FF_MIN_PWM`

### 3. 速度斜率标定

测试：

```text
KEY3 加速到 0.20、0.40、0.60 m/s
KEY2 逐步减速到 0
```

目标：

- CH5 跟随 CH4。
- CH8 跟随 CH7。
- CH6/CH9 不应长期顶到 `1000` 或 `-1000`。

调整：

- 高速段实际速度普遍低于目标：增大 `VEHICLE_SPEED_PI_FF_GAIN_PWM_PER_MPS`
- 高速段实际速度普遍高于目标：减小 `VEHICLE_SPEED_PI_FF_GAIN_PWM_PER_MPS`

### 4. KP 标定

在前馈基本正确后调 `VEHICLE_SPEED_PI_KP`。

现象：

- 响应慢、误差大：增大 KP。
- 速度曲线来回振荡、PWM 抖动明显：减小 KP。

建议一次只改 20% 左右。

### 5. I 项按需启用

只有当前馈和 KP 都稳定，但稳态仍有明显误差时再开启：

```c
#define VEHICLE_SPEED_PI_I_ENABLE 1
```

注意：

- KI 从小值开始。
- 如果停车后 PWM 仍残留、加速后明显过冲，减小 KI 或 I_MAX。

## 每轮测试流程

推荐一轮测试顺序：

```text
1. TEST_SELECT 改为 TEST_SPEED_PID
2. 编译烧录
3. KEY1 确认速度为 0
4. KEY3 逐步加速
5. KEY2 逐步减速
6. KEY1 清零停车
```

每次只改一个参数，记录 CH4/CH5/CH6 和 CH7/CH8/CH9 的变化。

## 当前实车标定记录

记录时间：2026-07-11

### 1. 开环最大速度测试

曾临时将速度测试改为开环映射：

```text
目标速度 0 -> PWM 0
目标速度 VEHICLE_MAX_SPEED_MPS -> PWM 1000
```

实测满 PWM 数据：

- 左右轮 PWM：`1000`
- 车身实际速度：约 `6.2 m/s`
- 左右轮速度接近，无明显单侧掉速
- `fault_mask = 64` 为 `motion_limited`，表示 PWM 达到限幅，属于满 PWM 测速时的正常现象

结论：

- 驱动和编码器在满 PWM 下工作正常。
- `6.2 m/s` 可作为当前电池、电机、轮径、地面条件下的开环最高速度参考。
- 闭环标定仍按 `VEHICLE_MAX_SPEED_MPS = 1.0f` 做低速可控区间，不直接使用最高速度作为控制上限。

### 2. 当前稳定参数

当前速度环参数记录在 `App/Inc/vehicle_config.h`：

```c
#define VEHICLE_MAX_SPEED_MPS                 1.0f

#define VEHICLE_SPEED_PI_KP                   280.0f
#define VEHICLE_SPEED_PI_KI                   30.0f
#define VEHICLE_SPEED_PI_I_ENABLE             1
#define VEHICLE_SPEED_PI_I_MAX                80.0f

#define VEHICLE_SPEED_PI_TARGET_DEADBAND      0.01f
#define VEHICLE_SPEED_PI_FF_MIN_PWM           480.0f
#define VEHICLE_SPEED_PI_FF_GAIN_PWM_PER_MPS  100.0f
```

调参过程：

- 初始前馈偏大，`0.8 m/s` 和 `1.0 m/s` 实测速度长期偏高。
- 将 `FF_MIN` 逐步降到 `480`，将 `FF_GAIN` 降到 `100`。
- `Kp` 从 `200` 提高到 `280` 后，PWM 能随速度误差更明显地修正。
- 开启 I 项后，`Ki=60 / I_MAX=120` 推得偏猛，容易到 `1.10 m/s` 台阶。
- 最终改为 `Ki=30 / I_MAX=80`，保留慢速修正能力，同时减少过冲。

### 3. 当前 1.0 m/s 测试现象

目标：

```text
CH2 / CH4 / CH7 = 1.0 m/s
```

典型反馈：

- CH5 / CH8 多数在 `1.021 m/s`
- 偶尔出现 `0.942 m/s`
- PWM 多数在 `584 ~ 608`
- `fault_mask = 0`

判断：

- 速度环已经闭环工作，PWM 会根据实际速度误差变化。
- 当前效果比 `Ki=60` 更稳，没有明显频繁冲到 `1.10 m/s`。
- 由于编码器量化，速度显示会在固定台阶之间跳变，不能期待稳定显示精确 `1.000 m/s`。

### 4. 后续验证点

下一轮不要只看 `1.0 m/s`，需要逐点确认：

```text
0.3 m/s
0.5 m/s
0.8 m/s
1.0 m/s
```

通过标准：

- 实际速度围绕目标上下跳，不长期偏一边。
- 左右轮实际速度接近。
- CH6 / CH9 PWM 不长期顶到 `1000`。
- `fault_mask` 保持 `0`。

如果低速 `0.3 m/s` 仍然明显冲或抖：

- 优先检查 `FF_MIN` 是否仍偏大。
- 其次再考虑低速死区或最小 PWM 分段补偿。

如果中高速 `0.8 ~ 1.0 m/s` 长期偏低：

- 优先小幅增加 `Kp` 或 `FF_GAIN`。
- 每次只改一个参数，避免无法判断来源。
