# V3 功能开发计划

当前工程 V2 主体框架已经完成。V3 阶段目标是基于稳定框架继续开发功能：完成“蓝牙手动控制闭环 + 自动任务入口 + 巡线第一版”。

## 1. 当前基础

当前主链路：

```text
BSP
  -> SystemStatePool
  -> Sensor / Estimation / Comm / HMI
  -> Decision
  -> Motion
  -> Debug
```

当前状态：

| 项目 | 状态 |
|---|---|
| BSP 底层驱动 | 已完成基础验证 |
| V2 主体框架 | 已完成 |
| 老框架清理 | 已完成第一轮 |
| 测试 demo | 已迁移主要 V2 链路 |
| Debug 曲线 | 已迁移到 `Modules/Debug` |
| 手动控制协议 | 待实现 |
| 自动任务入口 | 待实现 |
| 巡线业务逻辑 | 待实车调试 |

## 2. 阶段 A：蓝牙手动控制

目标：上位机通过蓝牙 UART 发送速度和角度，车辆进入手动模式并执行动作。

### 2.1 协议格式

```text
速度,角度
```

示例：

```text
300,0
300,-500
300,500
-300,0
0,0
```

约定：

| 字段 | 范围 | 含义 |
|---|---:|---|
| 速度 | `-1000 ~ 1000` | 负数后退，正数前进 |
| 角度 | `-1000 ~ 1000` | 负数左转，正数右转 |

### 2.2 实现任务

- [ ] 在 `Modules/Comm/Src/bt_link.c` 中解析 `xxx,xxx`。
- [ ] 解析成功后生成 `BT_COMMAND_MANUAL_MOVE`。
- [ ] `arg0` 保存速度千分比。
- [ ] `arg1` 保存角度千分比。
- [ ] 在 `Modules/Decision/Src/decision.c` 中处理手动命令。
- [ ] 手动模式下写入 `pool->target.speed_mps`。
- [ ] 手动模式下写入 `pool->target.steer_angle_rad`。
- [ ] 增加蓝牙手动超时保护，超过 500ms 未收到新手动命令则停车。

### 2.3 映射建议

```c
speed_mps = speed_permille * 0.001f;
steer_rad = steer_permille * 0.0005f;
```

### 2.4 验收标准

| 输入 | 期望动作 |
|---|---|
| `300,0` | 前进 |
| `-300,0` | 后退 |
| `300,-500` | 前进左转 |
| `300,500` | 前进右转 |
| `0,0` | 停车 |
| 停止发送超过 500ms | 自动停车 |

## 3. 阶段 B：V2 速度环实车标定

目标：在手动控制下让左右轮速度响应稳定。

### 3.1 调试曲线

当前 `Modules/Debug/Src/debug_trace.c` 通过蓝牙输出 10 通道小端浮点数组：

```text
10 * float32 little-endian + 00 00 80 7F
```

通道：

| 通道 | 含义 |
|---|---|
| CH1 | mode |
| CH2 | target_body_mps |
| CH3 | actual_body_mps |
| CH4 | left_target_mps |
| CH5 | left_actual_mps |
| CH6 | left_pwm |
| CH7 | right_target_mps |
| CH8 | right_actual_mps |
| CH9 | right_pwm |
| CH10 | fault_mask |

### 3.2 实现任务

- [ ] 使用手动控制指令测试低速、中速、后退。
- [ ] 观察左右目标速度和实际速度是否同向。
- [ ] 标定 `Modules/Motion/Src/speed_pi.c` 中的 `FF_MIN_PWM`。
- [ ] 标定 `FF_GAIN_PWM_PER_MPS`。
- [ ] 标定 `KP`。
- [ ] 评估是否启用 `KI`。
- [ ] 增加单轮无反馈保护。

### 3.3 单轮无反馈保护建议

条件：

```text
目标速度不为 0
PWM 已经较大
对应编码器速度接近 0
持续 500ms
```

动作：

```text
置 fault
停车
Debug 曲线 fault_mask 置位
```

### 3.4 验收标准

- [ ] 左右轮实际速度能跟随目标。
- [ ] PWM 不长期满输出。
- [ ] 低速不明显卡顿。
- [ ] 前进和后退方向正确。
- [ ] 编码器无反馈时能进入保护。

## 4. 阶段 C：自动任务入口

目标：建立统一的自动任务选择机制，后续巡线、巡线加避障、巡检都通过这个入口启动。

### 4.1 自动任务枚举建议

```c
AUTO_TASK_NONE
AUTO_TASK_LINE_FOLLOW
AUTO_TASK_LINE_FOLLOW_OBSTACLE
AUTO_TASK_INSPECTION
```

### 4.2 蓝牙命令建议

```text
AUTO,1
AUTO,2
STOP
MANUAL
```

含义：

| 命令 | 含义 |
|---|---|
| `AUTO,1` | 巡线 |
| `AUTO,2` | 巡线 + 避障 |
| `STOP` | 停车 |
| `MANUAL` | 回到手动模式 |

### 4.3 实现任务

- [ ] 在 `SystemStatePool` 中增加自动任务状态。
- [ ] 在 `BtCommandType` 中增加自动任务命令或复用现有 `BT_COMMAND_START_TASK`。
- [ ] 在 `Comm` 中解析 `AUTO,n`。
- [ ] 在 `Decision` 中根据任务 ID 切换模式。
- [ ] 在 `Decision` 中实现 `STOP` 和 `MANUAL` 命令。

### 4.4 验收标准

- [ ] `STOP` 能立即停车。
- [ ] `MANUAL` 能进入手动模式。
- [ ] `AUTO,1` 能进入巡线模式。
- [ ] `AUTO,2` 能进入巡线加避障预留模式。

## 5. 阶段 D：巡线第一版

目标：低速完成基础巡线。

### 5.1 输入

来自 `Sensor`：

```c
pool->sensor.track_error
pool->sensor.track_valid
pool->sensor.track_bits
```

### 5.2 输出

由 `Decision` 写入：

```c
pool->target.speed_mps
pool->target.steer_angle_rad
pool->target.valid
```

### 5.3 控制策略第一版

```c
speed = 0.2f;
steer = track_error * LINE_KP;
```

建议从低速开始：

```c
speed = 0.2f ~ 0.3f;
```

### 5.4 丢线策略

第一版建议：

```text
track_valid=false -> 停车
```

后续可升级为：

```text
短时间丢线 -> 保持上一次方向低速搜索
长时间丢线 -> 停车报警
```

### 5.5 实现任务

- [ ] 确认 `track_error` 左右方向和舵机方向一致。
- [ ] 标定 `LINE_KP`。
- [ ] 固定低速巡线。
- [ ] 丢线停车。
- [ ] Debug 曲线增加 `track_error` 或临时替换某个通道观察。

### 5.6 验收标准

- [ ] 能沿线低速运行。
- [ ] 左右转向方向正确。
- [ ] 丢线后不会继续乱跑。
- [ ] `LINE_KP` 可通过实测继续调。

## 6. 阶段 E：巡线 + 避障

目标：在巡线基础上加入超声波避障策略。

### 6.1 输入

```c
pool->sensor.ultrasonic_mm
pool->sensor.ultrasonic_valid
pool->sensor.obstacle_near
```

### 6.2 第一版策略

```text
无障碍 -> 正常巡线
近距离障碍 -> 停车
障碍消失 -> 恢复巡线
```

### 6.3 后续策略

```text
近障碍 -> 减速
非常近 -> 停车
障碍持续存在 -> 等待或切换绕行任务
```

### 6.4 验收标准

- [ ] 巡线过程中遇到障碍能停车。
- [ ] 障碍解除后能继续巡线。
- [ ] 超声波无效时不会误判为近障碍。

## 7. 推荐执行顺序

```text
1. 蓝牙 xxx,xxx 手动控制
2. 手动控制实车验证
3. V2 速度环参数标定
4. 自动任务命令入口
5. 巡线第一版
6. 巡线 + 避障
```

## 8. 当前优先级

最高优先级：

```text
蓝牙 xxx,xxx 手动控制
```

原因：

```text
手动控制能验证 Comm -> Decision -> Motion 全链路。
速度环标定也需要稳定的手动输入。
自动任务最终也会复用同一套 Motion 执行链路。
```
