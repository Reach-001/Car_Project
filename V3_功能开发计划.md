# V3 功能开发计划

当前工程 V2 主体框架已经完成。V3 阶段目标是基于稳定框架继续开发功能：完成“蓝牙手动控制闭环 + 自动任务入口 + 巡线第一版”。

## 1. 当前基础

当前主链路：

```text
BSP
  -> SystemStatePool
  -> Sensor / Estimation / Comm / HMI
  -> Decision
  -> Motion
  -> Debug
```

当前状态：

| 项目 | 状态 |
|---|---|
| BSP 底层驱动 | 已完成基础验证 |
| V2 主体框架 | 已完成 |
| 老框架清理 | 已完成第一轮 |
| 测试 demo | 已迁移主要 V2 链路 |
| Debug 曲线 | 已迁移到 `Modules/Debug` |
| 手动控制协议 | 已实现，待主链路实车联调 |
| 自动任务入口 | 已实现第一版 |
| 巡线业务逻辑 | 已实现第一版，待实车调参 |
| 速度环实车标定 | 已完成第一版参数 |
| 阿克曼几何模型 | 已参数化，待实车量轴距/轮距 |

## 2. 阶段 A：蓝牙手动控制

目标：上位机通过蓝牙 UART 发送速度和角度，车辆进入手动模式并执行动作。

### 2.1 协议格式

```text
速度,角度
STOP
DEBUG,0
DEBUG,1
```

示例：

```text
300,0
60,-30
60,30
-60,0
0,0
STOP
DEBUG,1
DEBUG,0
```

约定：

| 字段 | 范围 | 含义 |
|---|---:|---|
| 速度 | `-100 ~ 100` | 负数后退，正数前进 |
| 角度 | `-45 ~ 45` 度 | 前轮相对机械中位的目标角度，负数左转，正数右转 |

系统默认处于手动模式，不需要发送 `MANUAL`。收到新的 `speed,angle` 后锁存该目标；未收到新目标时保持上一组目标。发送 `0,0` 或 `STOP` 停车。

转向角度还未实车标定，相关参数集中在：

```text
App/Inc/vehicle_config.h
```

优先修改：

```c
VEHICLE_MANUAL_ANGLE_MIN_DEG
VEHICLE_MANUAL_ANGLE_MAX_DEG
VEHICLE_STEER_MAX_DEG
```

### 2.2 实现任务

- [x] 在 `Modules/Comm/Src/bt_link.c` 中解析 `speed,angle`。
- [x] 在 `Modules/Comm/Src/bt_link.c` 中解析 `STOP`。
- [x] 在 `Modules/Comm/Src/bt_link.c` 中解析 `DEBUG,0/1`。
- [x] 解析成功后生成 `BT_COMMAND_MANUAL_MOVE`。
- [x] `arg0` 保存速度，范围 `-100~100`。
- [x] `arg1` 保存角度，范围由 `VEHICLE_MANUAL_ANGLE_MIN_DEG / MAX_DEG` 限制。
- [x] 在 `Modules/Decision/Src/decision.c` 中处理手动命令。
- [x] 手动模式下写入 `pool->target.speed_mps`。
- [x] 手动模式下写入 `pool->target.steer_angle_rad`。
- [x] 手动目标锁存，未收到新 `speed,angle` 时保持上一状态。

### 2.3 映射建议

```c
speed_mps = speed_percent * VEHICLE_MANUAL_MAX_SPEED_MPS / 100;
steer_rad = angle_deg * VEHICLE_DEG_TO_RAD;
```

### 2.4 验收标准

| 输入 | 期望动作 |
|---|---|
| `60,0` | 前进 |
| `-60,0` | 后退 |
| `60,-30` | 前进左转 |
| `60,30` | 前进右转 |
| `0,0` | 停车 |
| `STOP` | 停车并进入 STOP 模式 |

## 3. 阶段 B：V2 速度环实车标定

目标：在手动控制下让左右轮速度响应稳定。

### 3.1 调试曲线

当前 `Modules/Debug/Src/debug_trace.c` 通过蓝牙输出 10 通道小端浮点数组：

```text
10 * float32 little-endian + 00 00 80 7F
```

默认不输出曲线，避免干扰手动控制串口。通过蓝牙命令开关：

```text
DEBUG,1  开启曲线输出
DEBUG,0  关闭曲线输出
```

通道：

| 通道 | 含义 |
|---|---|
| CH1 | mode |
| CH2 | target_body_mps |
| CH3 | left_encoder_delta |
| CH4 | left_target_mps |
| CH5 | left_actual_mps |
| CH6 | left_pwm_norm |
| CH7 | right_target_mps |
| CH8 | right_actual_mps |
| CH9 | right_pwm_norm |
| CH10 | right_encoder_delta |

### 3.2 实现任务

- [x] 使用 Debug 测试入口完成速度环第一版实车标定。
- [ ] 使用主链路手动控制指令测试低速、中速、后退。
- [ ] 观察左右目标速度和实际速度是否同向。
- [x] 标定 `FF_MIN_PWM`。
- [x] 标定 `FF_GAIN_PWM_PER_MPS`。
- [x] 标定 `KP`。
- [x] 启用并初步标定 `KI`。
- [ ] 增加单轮无反馈保护。

当前速度环第一版参数：

```c
VEHICLE_SPEED_PI_FF_MIN_PWM = 480.0f
VEHICLE_SPEED_PI_FF_GAIN_PWM_PER_MPS = 100.0f
VEHICLE_SPEED_PI_KP = 280.0f
VEHICLE_SPEED_PI_KI = 30.0f
VEHICLE_SPEED_PI_I_ENABLE = 1
VEHICLE_SPEED_PI_I_MAX = 80.0f
```

### 3.3 单轮无反馈保护建议

条件：

```text
目标速度不为 0
PWM 已经较大
对应编码器速度接近 0
持续 500ms
```

动作：

```text
置 fault
停车
Debug 曲线 fault_mask 置位
```

### 3.4 验收标准

- [ ] 左右轮实际速度能跟随目标。
- [ ] PWM 不长期满输出。
- [ ] 低速不明显卡顿。
- [ ] 前进和后退方向正确。
- [ ] 编码器无反馈时能进入保护。

## 3.5 阶段 B 补充：阿克曼几何模型

目标：将车身速度和前轮转角转换为左右驱动轮目标速度。

当前模型：

```text
R = wheelbase / tan(steer)
left_speed  = body_speed * (1 - track_width / (2R))
right_speed = body_speed * (1 + track_width / (2R))
```

参数位置：

```c
VEHICLE_WHEELBASE_M
VEHICLE_TRACK_WIDTH_M
VEHICLE_ACKERMANN_RATIO_LIMIT
```

当前状态：

- [x] 阿克曼模型已从硬编码迁移到 `vehicle_config.h`
- [x] 已增加左右轮速比例差限幅，防止大转角时内侧轮目标异常
- [ ] 实测轴距并修改 `VEHICLE_WHEELBASE_M`
- [ ] 实测轮距并修改 `VEHICLE_TRACK_WIDTH_M`
- [ ] 通过低速大角度转弯检查左右轮目标速度是否合理

## 4. 阶段 C：自动任务入口

目标：建立统一的自动任务选择机制，后续巡线、巡线加避障、巡检都通过这个入口启动。

### 4.1 自动任务枚举建议

```c
AUTO_TASK_NONE
AUTO_TASK_LINE_FOLLOW
AUTO_TASK_LINE_FOLLOW_OBSTACLE
AUTO_TASK_INSPECTION
```

### 4.2 蓝牙命令建议

```text
AUTO,1
AUTO,2
STOP
MANUAL
```

含义：

| 命令 | 含义 |
|---|---|
| `AUTO,1` | 巡线 |
| `AUTO,2` | 巡线 + 避障 |
| `STOP` | 停车 |
| `MANUAL` | 回到手动模式 |

### 4.3 按键入口

| 按键 | 功能 |
|---|---|
| KEY1 | 停车，进入 STOP |
| KEY2 | 手动 / 巡线模式切换 |
| KEY3 | 启动 `AUTO_TASK_LINE_FOLLOW` |
| KEY4 / User_Key 短按 | 切换 Debug 曲线输出 |
| KEY4 / User_Key 长按 | 强制打开 Debug 曲线输出 |

### 4.4 实现任务

- [ ] 在 `SystemStatePool` 中增加自动任务状态。
- [x] 在 `SystemStatePool` 中增加自动任务状态。
- [x] 在 `BtCommandType` 中复用 `BT_COMMAND_START_TASK` 作为自动任务命令。
- [x] 在 `Comm` 中解析 `AUTO,n`。
- [x] 在 `Decision` 中根据任务 ID 切换模式。
- [x] 在 `Decision` 中实现 `STOP` 和 `speed,angle` 命令。
- [x] KEY2 支持手动 / 巡线模式切换。
- [x] KEY3 支持启动巡线任务。
- [x] KEY4 短按/长按支持 Debug 输出控制。

### 4.5 验收标准

- [ ] `STOP` 能立即停车。
- [ ] `MANUAL` 能进入手动模式。
- [x] `AUTO,1` 能进入巡线模式。
- [x] `AUTO,2` 能进入巡线加避障预留模式。

## 5. 阶段 D：巡线第一版

目标：低速完成基础巡线。

### 5.1 输入

来自 `Sensor`：

```c
pool->sensor.track_error
pool->sensor.track_valid
pool->sensor.track_bits
```

### 5.2 输出

由 `Decision` 写入：

```c
pool->target.speed_mps
pool->target.steer_angle_rad
pool->target.valid
```

### 5.3 当前控制策略

当前由 `Modules/Decision/Src/decision.c` 计算巡线目标：

- 有线：`track_error` 经过 `KP + KD` 计算转向角。
- 弯道：误差越大，速度从 `VEHICLE_LINE_FOLLOW_SPEED_MPS` 向 `VEHICLE_LINE_FOLLOW_MIN_SPEED_MPS` 降低。
- 短暂丢线：`VEHICLE_LINE_FOLLOW_LOST_HOLD_MS` 内保持最后一次方向。
- 丢线搜索：超过 HOLD 后以 `VEHICLE_LINE_FOLLOW_SEARCH_SPEED_MPS` 低速按最后方向找线。
- 丢线停车：超过 `VEHICLE_LINE_FOLLOW_LOST_STOP_MS` 后停车。

参数集中在 `App/Inc/vehicle_config.h`：

```c
VEHICLE_LINE_FOLLOW_SPEED_MPS
VEHICLE_LINE_FOLLOW_MIN_SPEED_MPS
VEHICLE_LINE_FOLLOW_STEER_SIGN
VEHICLE_LINE_FOLLOW_KP
VEHICLE_LINE_FOLLOW_KD
VEHICLE_LINE_FOLLOW_SLOWDOWN_GAIN
VEHICLE_LINE_FOLLOW_LOST_HOLD_MS
VEHICLE_LINE_FOLLOW_LOST_STOP_MS
VEHICLE_LINE_FOLLOW_SEARCH_SPEED_MPS
VEHICLE_LINE_FOLLOW_SEARCH_STEER_DEG
```

### 5.4 Debug 曲线

开启：

```text
DEBUG,1
```

巡线模式下仍保持 10 通道帧长，其中：

| 通道 | 含义 |
|---|---|
| CH1 | mode |
| CH2 | target_body_mps |
| CH3 | left_encoder_delta |
| CH4 | left_target_mps |
| CH5 | left_actual_mps |
| CH6 | left_pwm_norm |
| CH7 | right_target_mps |
| CH8 | right_actual_mps |
| CH9 | right_pwm_norm |
| CH10 | right_encoder_delta |

### 5.5 实现任务

- [ ] 确认 `track_error` 左右方向和舵机方向一致。
- [ ] 标定 `LINE_KP` / `LINE_KD`。
- [x] 巡线目标计算接入主链路。
- [x] 弯道减速。
- [x] 短暂丢线保持。
- [x] 丢线搜索。
- [x] 长时间丢线停车。
- [x] Debug 曲线增加左右编码器 delta，CH6/CH9 保留左右 PWM 归一化值。

### 5.6 验收标准

- [ ] 能沿线低速运行。
- [ ] 左右转向方向正确。
- [ ] 丢线后不会继续乱跑。
- [ ] `LINE_KP` 可通过实测继续调。

## 6. 阶段 E：巡线 + 避障

目标：在巡线基础上加入超声波避障策略。

### 6.1 输入

```c
pool->sensor.ultrasonic_mm
pool->sensor.ultrasonic_valid
pool->sensor.obstacle_near
```

### 6.2 第一版策略

```text
无障碍 -> 正常巡线
近距离障碍 -> 停车
障碍消失 -> 恢复巡线
```

### 6.3 后续策略

```text
近障碍 -> 减速
非常近 -> 停车
障碍持续存在 -> 等待或切换绕行任务
```

### 6.4 验收标准

- [ ] 巡线过程中遇到障碍能停车。
- [ ] 障碍解除后能继续巡线。
- [ ] 超声波无效时不会误判为近障碍。

## 7. 推荐执行顺序

```text
1. 蓝牙 xxx,xxx 手动控制
2. 手动控制实车验证
3. V2 速度环参数标定
4. 自动任务命令入口
5. 巡线第一版
6. 巡线 + 避障
```

## 8. 当前优先级

最高优先级：

```text
蓝牙 xxx,xxx 手动控制
```

原因：

```text
手动控制能验证 Comm -> Decision -> Motion 全链路。
速度环标定也需要稳定的手动输入。
自动任务最终也会复用同一套 Motion 执行链路。
```
