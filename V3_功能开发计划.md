# V3 功能开发计划

当前状态：V2 架构完成（7 域 + SystemStatePool + BSP），骨架代码就绪。

---

## Phase 1：蓝牙协议解析（bt_link.c 重写）

**输入格式**：上位机通过蓝牙串口发送 `xxx,xxx\r\n`
- 第一个值 = 速度（千分比 -1000~1000，负=后退）
- 第二个值 = 角度（千分比 -1000~1000，负=左转）

**改动文件**：`Modules/Comm/Src/bt_link.c`

**实现**：
1. 去掉 ProtocolLineParser，直接在收字节时实时解析
2. 状态机：WAIT_DIGIT → PARSE_SPEED → WAIT_COMMA → PARSE_ANGLE → WAIT_CR
3. 解析完成后填充 `BtCommand{type=BT_COMMAND_MANUAL_MOVE, arg0=speed, arg1=angle}`
4. 校验：范围检查，非法值丢弃
5. 超时：连续 500ms 无新数据 → 复位解析状态（避免半个数字残留）

**验收标准**：串口助手发 `300,200` 回车 → `CommState.bt_cmd.arg0=300, arg1=200`

---

## Phase 2：手动遥控模式完善

**改动文件**：`Modules/Decision/Src/decision.c`

**实现**：
1. MANUAL 模式持续读取 `pool->comm.bt_command`
2. 蓝牙数据超时安全：如果 MANUAL 模式下 500ms 未收到新数据 → 自动切到 STOP
3. 速度/角度缩放已就绪（千分比 → m/s, rad），只需标定最大速度

**验收标准**：手机蓝牙发 `500,0` → 车匀速前进；发 `0,-300` → 原地左转

---

## Phase 3：自动循迹控制完善

**改动文件**：`Modules/Decision/Src/decision.c`、循迹 PID 参数

**实现**：
1. LINE_FOLLOW 模式下持续读取 `pool->sensor.track_error`
2. PD 控制：error 比例 + error 微分，减少振荡
3. 分档速度：偏差小时加速、偏差大时减速
4. 脱线保护：连续 N 帧 `track_valid=false` → 停车报警
5. 标定步骤：
   - 实车跑直线，观察 `track_error` 波动范围
   - 调 KP 使转向响应跟上线
   - 加 KD 消除摆动
   - 调目标速度使整体稳

**验收标准**：放黑线上跑，车稳定沿线上前进

---

## Phase 4：按键操作逻辑

**改动文件**：`Modules/HMI/Src/key_service.c`、`Modules/HMI/Src/led_service.c`

**按键定义**：
| 按键 | 短按 | 长按 (1s+) |
|---|---|---|
| KEY1 | 紧急停车 → STOP | — |
| KEY2 | 模式轮转 STOP→MANUAL→LINE_FOLLOW | — |
| KEY3 | 任务启停（自动模式） | — |
| KEY4 | 蜂鸣器静音/恢复 | 用户自定义 |

**实现**：
1. KEY1 短按 → `pool->event.key_stop_clicked`（已实现）
2. KEY2 短按 → `pool->event.key_mode_clicked` → Decision 切换模式（已实现）
3. KEY3 短按 → `pool->event.key_task_clicked` → 启停自动任务
4. KEY4 短按 → 切蜂鸣器静音标志
5. 通过 `BspKey_GetInfo().pressed_time_ms` 判断长按

**验收标准**：按 KEY2，LED 显示从 STOP（STATE_LED 闪）→ MANUAL（LED1 常亮）→ LINE_FOLLOW（LED1+2 常亮）→ 循环

---

## Phase 5：自动巡检任务状态机

**改动文件**：`Modules/Decision/Src/decision.c`（新增 inspect_fsm.c）

**任务流程**（以简单巡检为例）：
```
START → 前进 1s → 减速 → 检测 K230 标记
  → 发现标记 → 原地右转 180° → 返回
  → 无标记 → 继续前进 → 超声波障碍 → 停车 2s → 后退 1s → 转 90° → 继续
```

**状态机**：
| 状态 | 动作 | 转换条件 |
|---|---|---|
| IDLE | 停车等待 | 任务启动命令 |
| FORWARD | 0.3m/s 直行 | 发现标记 / 障碍 / 超时 |
| TURN_RIGHT | 原地右转 90° | 陀螺仪/编码器计角度 |
| TURN_AROUND | 原地 180° | 完成 |
| RETURN | 返程 | 到起点 / 超声波 |
| AVOID | 后退 1s → 转 90° | 前进 |

**依赖**：
- K230 模块提供标记检测（`pool->k230_result.type == K230_RESULT_MARKER`）
- 超声波提供障碍检测（`pool->sensor.obstacle_near`）
- 编码器提供里程计（`pool->estimation.left_encoder_delta` 累计）

**验收标准**：放空旷地，启动任务后小车自动前跑→遇障停车→绕障→继续

---

## Phase 6：系统集成 + 异常处理

**改动**：`App/app_main.c`、`App/app_tasks.c`

**实现**：
1. 看门狗确保喂够（当前 500ms，足够）
2. 通信断线保护：蓝牙 >500ms 无数据 → STOP
3. 传感器故障降级：循迹失效 → 无法 LINE_FOLLOW → 切 STOP
4. 电机堵转保护：target>0 但编码器 delta=0 持续 500ms → STOP
5. 电池低压检测（预留 ADC）
6. 启动时蜂鸣器自检：短响一声 = OK

**验收标准**：系统上电→蜂鸣 OK → 正常跑；任意故障→安全停车+蜂鸣报警

---

## 实施顺序

```
Phase 1 (bt_link 解析)  ← 第一优先，打通蓝牙遥控
    ↓
Phase 2 (手动遥控)     ← 依赖 Phase 1
    ↓
Phase 4 (按键操作)     ← 低耦合，可并行
    ↓
Phase 3 (循迹)         ← 需要实车标定
    ↓
Phase 5 (巡检)         ← 最复杂，最后做
    ↓
Phase 6 (集成)         ← 收尾
```

---

## 估时

| Phase | 内容 | 文件数 |
|---|---|---|
| 1 | bt_link 协议解析 | 1 文件改动 |
| 2 | 手动遥控完善 | 1 文件改动 |
| 3 | 循迹控制 | 1~2 文件改动 |
| 4 | 按键逻辑 | 2 文件改动 |
| 5 | 巡检状态机 | 新增 1~2 文件 |
| 6 | 系统集成 | 小幅修改 |

**Phase 1~2 完成后就能用手柄/手机遥控开车。Phase 3~5 是自动化场景。**

---

## 需要你确认的

1. 蓝牙上位机发出的格式是 `speed,angle\r\n` 吗？有没有帧头帧尾校验？
2. K230 识别结果的协议格式定义了吗？还是先占位后续补充？
3. 自动巡检的具体路线和任务逻辑是你来定还是我按通用逻辑写？
