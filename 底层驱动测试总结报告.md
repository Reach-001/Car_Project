# 底层驱动测试总结报告

日期：2026-07-10  
平台：STM32G431，CMake + STM32Cube HAL  
当前阶段：BSP/底层外设测试完成，准备进入上层逻辑编写

## 1. 当前工程状态

- 当前测试入口：`App/Inc/app_test.h`
- 当前测试选择：`TEST_SELECT 8`，即 UART 测试模式
- 正常整车逻辑入口：
  - `App_Init()`
  - `App_Run()`
- 正常运行模式需要将 `TEST_SELECT` 改为 `TEST_NONE`

构建验证：

```bash
cmake --build --preset Debug -j 8
```

当前构建已通过。

## 2. 已完成底层测试项目

| 模块 | 状态 | 结论 |
|---|---:|---|
| LED | 通过 | 四路 LED 可独立控制 |
| 按键 | 通过 | 10ms 消抖、事件读取正常 |
| 蜂鸣器 | 通过 | TIM7 中断翻转 GPIO，可发声和播放提示音 |
| 电机 | 通过 | 左右电机方向、PWM 调速、差速转弯已验证 |
| 舵机 | 通过 | TIM1 CH3 输出 50Hz PWM，千分比转角可控 |
| 编码器 | 通过 | TIM2/TIM4 编码器模式可读取增量 |
| 循迹 | 通过 | 五路 GPIO 输入可读，PB8/BOOT0 问题已处理 |
| 超声波 | 通过 | 改为带超时轮询方式，避免 EXTI/等待卡死 |
| UART | 通过调试中 | 已改为 DMA RX 轮询搬运，支持短包；测试模式为双向桥接 |

## 3. 引脚与外设映射

### LED

| 名称 | 引脚 | 说明 |
|---|---|---|
| LED1 | PA4 | 高电平点亮 |
| LED2 | PA5 | 高电平点亮 |
| LED3 | PC4 | 高电平点亮 |
| STATE_LED | PC6 | 高电平点亮，建议用于心跳/状态 |

### 按键

| 名称 | 引脚 | 有效电平 |
|---|---|---|
| KEY1 | PA11 | 低电平按下 |
| KEY2 | PA12 | 低电平按下 |
| KEY3 | PA15 | 低电平按下 |
| KEY4/User_Key | PC13 | 高电平按下 |

### 电机驱动 AT8236

| 电机 | IN1 | IN2 | 定时器通道 |
|---|---|---|---|
| 左电机 | PA6 | PA7 | TIM3_CH1 / TIM3_CH2 |
| 右电机 | PB0 | PB1 | TIM3_CH3 / TIM3_CH4 |

PWM 频率：约 20kHz。  
占空比接口范围：`-1000 ~ 1000`。

实测方向：

| 命令 | 车辆含义 |
|---|---|
| 正数 | 前进方向 |
| 负数 | 后退方向 |
| 0 | 停止/滑行 |

已标定差速：

| 左电机 | 右电机 | 效果 |
|---:|---:|---|
| 900 | 600 | 左转 |
| 600 | 900 | 右转 |

### 舵机

| 名称 | 引脚 | 定时器 |
|---|---|---|
| SERVO_PWM | PA10 | TIM1_CH3 |

PWM 周期：20ms。  
接口范围：`-1000 ~ 1000`，内部映射约为 `1000us ~ 2000us`。

### 编码器

| 编码器 | A 相 | B 相 | 定时器 |
|---|---|---|---|
| 左轮 | PA0 | PA1 | TIM2 |
| 右轮 | PB6 | PB7 | TIM4 |

### 循迹

| 传感器 | 引脚 | 说明 |
|---|---|---|
| TRACK_1 | PB3 | 高电平表示检测到黑线 |
| TRACK_2 | PB4 | 高电平表示检测到黑线 |
| TRACK_3 | PB5 | 高电平表示检测到黑线 |
| TRACK_4 | PB8/BOOT0 | 高电平表示检测到黑线 |
| TRACK_5 | PB9 | 高电平表示检测到黑线 |

注意：

- `PB8` 同时是 `BOOT0` 引脚。
- 已通过 Option Bytes 配置：
  - `nSWBOOT0 = 0`
  - `nBOOT0 = 1`
- 这表示启动不再取 PB8/BOOT0 外部电平，避免 TRACK_4 上电高电平导致进入 Bootloader。
- GPIO 配置上已给五路循迹输入加内部下拉。

### 超声波 HC-SR04

| 信号 | 引脚 | 当前驱动方式 |
|---|---|---|
| TRIG | PB2 | GPIO 输出 |
| ECHO | PB11 | GPIO 输入，下拉 |

当前实现：

- 不再依赖 EXTI 中断测距。
- 使用带超时保护的轮询测量。
- 避免 ECHO 悬空或异常时卡死主循环。

### UART

| 逻辑 ID | 外设 | TX | RX | 用途 |
|---|---|---|---|---|
| `BSP_UART_K230` | USART2 | PA2 | PA3 | K230/视觉模块 |
| `BSP_UART_BT` | USART3 | PC10 | PC11 | 蓝牙/调试串口 |

UART 参数：

- 115200 baud
- 8N1
- DMA RX circular
- DMA TX normal

UART 测试模式：

- `USART2_RX(PA3)` 收到的数据，从 `USART3_TX(PC10)` 发出。
- `USART3_RX(PC11)` 收到的数据，从 `USART2_TX(PA2)` 发出。
- LED1 表示 `USART2 -> USART3` 有转发。
- LED2 表示 `USART3 -> USART2` 有转发。

## 4. 主要接口速查

### LED

```c
void BspLed_Init(void);
void BspLed_Set(BspLedId led, bool on);
void BspLed_Toggle(BspLedId led);
```

常用：

```c
BspLed_Set(BSP_LED_STATE, true);
BspLed_Toggle(BSP_LED_STATE);
```

### 按键

```c
void BspKey_Init(void);
void BspKey_Task10ms(void);
bool BspKey_IsPressed(BspKeyId key);
bool BspKey_TakePressedEvent(BspKeyId key);
bool BspKey_TakeReleasedEvent(BspKeyId key);
bool BspKey_TakeClickedEvent(BspKeyId key);
BspKeyInfo BspKey_GetInfo(BspKeyId key);
```

使用规则：

- 必须每 10ms 调用 `BspKey_Task10ms()`。
- 事件接口是 Take 语义，读一次后自动清除。

### 蜂鸣器

```c
void BspBuzzer_Init(void);
void BspBuzzer_Set(bool on);
void BspBuzzer_SetFrequency(uint16_t frequency_hz);
void BspBuzzer_Beep(uint16_t on_ms);
void BspBuzzer_Play(BuzzerPattern pattern);
void BspBuzzer_Task10ms(void);
bool BspBuzzer_IsActive(void);
```

建议：

- 普通状态提示优先用 `BspBuzzer_Play()`。
- `BspBuzzer_Set(true)` 是连续鸣响，不建议长期阻塞式使用。
- 每 10ms 调用 `BspBuzzer_Task10ms()`。

### 电机

```c
void BspMotor_Init(void);
void BspMotor_SetDuty(BspMotorId motor, int16_t duty_permille);
void BspMotor_StopAll(void);
```

示例：

```c
// 直行
BspMotor_SetDuty(BSP_MOTOR_LEFT, 700);
BspMotor_SetDuty(BSP_MOTOR_RIGHT, 700);

// 左转，实车标定
BspMotor_SetDuty(BSP_MOTOR_LEFT, 900);
BspMotor_SetDuty(BSP_MOTOR_RIGHT, 600);

// 右转，实车标定
BspMotor_SetDuty(BSP_MOTOR_LEFT, 600);
BspMotor_SetDuty(BSP_MOTOR_RIGHT, 900);

// 停止
BspMotor_StopAll();
```

### 舵机

```c
bool BspServo_Init(void);
bool BspServo_IsAvailable(void);
void BspServo_SetSteerPermille(int16_t steer_permille);
```

示例：

```c
BspServo_SetSteerPermille(0);      // 中位
BspServo_SetSteerPermille(-500);   // 左侧
BspServo_SetSteerPermille(500);    // 右侧
```

后续建议：

- 阿克曼结构应以舵机转角作为主要转向量。
- 左右电机差速可作为辅助补偿，不建议做原地反向转向。

### 编码器

```c
void BspEncoder_Init(void);
BspEncoderSample BspEncoder_Read(void);
```

返回值：

```c
typedef struct
{
    int32_t left_count;
    int32_t right_count;
    int32_t left_delta;
    int32_t right_delta;
} BspEncoderSample;
```

建议：

- 固定 10ms 调用一次。
- 上层根据 `delta` 计算速度、闭环控制或里程。

### 循迹

底层原始读取：

```c
void BspGpioSensor_Init(void);
BspTrackRaw BspGpioSensor_ReadTrack(void);
```

模块层状态：

```c
void Tracker_Init(void);
void Tracker_Task10ms(void);
TrackerState Tracker_GetState(void);
void Tracker_SetActiveHigh(bool active_high);
```

`TrackerState`：

```c
typedef struct
{
    uint8_t bits;
    uint8_t active_count;
    int16_t error;
    bool line_detected;
    bool crossroad;
} TrackerState;
```

当前误差权重：

| 传感器 | 权重 |
|---|---:|
| TRACK_1 | -2000 |
| TRACK_2 | -1000 |
| TRACK_3 | 0 |
| TRACK_4 | 1000 |
| TRACK_5 | 2000 |

上层循迹建议：

- `line_detected == false`：丢线处理。
- `crossroad == true`：路口/大面积黑线处理。
- `error < 0` 和 `error > 0` 的实际转向方向需要结合实车再标定。

### 超声波

```c
void Ultrasonic_Init(void);
void Ultrasonic_Task10ms(void);
UltrasonicSample Ultrasonic_GetSample(void);
bool Ultrasonic_IsObstacleNear(uint16_t threshold_mm);
```

`UltrasonicSample`：

```c
typedef struct
{
    UltrasonicStatus status;
    uint16_t distance_mm;
    uint32_t echo_us;
    uint32_t last_update_ms;
    bool valid;
} UltrasonicSample;
```

使用建议：

```c
Ultrasonic_Task10ms();
if (Ultrasonic_IsObstacleNear(300)) {
    Chassis_Stop();
}
```

当前实现带超时保护；无回波时 `valid=false`，不会卡死。

### UART

```c
void BspUart_Init(BspUartId id);
bool BspUart_ReadByte(BspUartId id, uint8_t *byte);
uint16_t BspUart_Available(BspUartId id);
bool BspUart_WriteBuffer(BspUartId id, const uint8_t *data, uint16_t len);
bool BspUart_TxDone(BspUartId id);
void BspUart_WriteString(BspUartId id, const char *str);
```

使用规则：

- RX 为非阻塞读取。
- TX 为 DMA 异步发送。
- `BspUart_WriteBuffer()` 会把数据复制到驱动内部 TX 缓冲。
- 单次发送长度目前最大 128 字节。
- 再次发送前建议检查 `BspUart_TxDone(id)`。

示例：

```c
uint8_t b;
while (BspUart_ReadByte(BSP_UART_K230, &b)) {
    // feed parser
}

if (BspUart_TxDone(BSP_UART_BT)) {
    BspUart_WriteString(BSP_UART_BT, "OK\r\n");
}
```

## 5. 当前 App 调度结构

正常模式下 `AppTasks_Register()` 注册的任务：

| 任务 | 周期 | 内容 |
|---|---:|---|
| peripheral | 10ms | 按键、蜂鸣器、循迹、超声波 |
| chassis | 10ms | 编码器读取、电机/舵机输出 |
| comm | 10ms | K230/蓝牙通信处理 |
| mode | 20ms | 模式状态机 |
| input | 20ms | 按键输入处理 |
| heartbeat | 500ms | STATE_LED 心跳、喂狗 |

约束：

- 所有任务必须短小、非阻塞。
- 不要在任务里调用 `HAL_Delay()`。
- 串口、传感器、蜂鸣器都已按非阻塞思路整理。

## 6. 已解决的问题记录

### 6.1 电机方向与 PWM

问题：

- 初始测试只验证方向，未体现速度。
- 后续已确认 PWM 调速有效。

处理：

- 电机 PWM 映射修正为按 `ARR + 1` 计算。
- 已验证左右电机可独立控制速度。
- 阿克曼结构不使用左右反向原地转向，只保留同向差速辅助。

### 6.2 超声波卡死

问题：

- 初版超声波依赖 EXTI 双边沿和 DWT 微秒等待。
- ECHO 异常/浮空/DWT 异常时可能卡死。

处理：

- ECHO 改为下拉输入。
- 测距改为带超时的轮询方式。
- 不再依赖 EXTI 中断完成测距。

### 6.3 PB8/BOOT0 与 TRACK_4 冲突

问题：

- TRACK_4 使用 PB8，而 PB8 同时是 BOOT0。
- 上电时 PB8 被拉高会影响启动。

处理：

- 五路循迹输入加 GPIO 下拉。
- Option Bytes 已设置：
  - `nSWBOOT0=0`
  - `nBOOT0=1`
- 启动不再取 PB8/BOOT0 引脚电平。

验证命令：

```bash
./tools/read_option_bytes.sh
```

写入命令：

```bash
./tools/program_boot0_option_bytes.sh
```

### 6.4 UART 短包收不到

问题：

- 初版 DMA RX 只在半满 128 字节或全满 256 字节时搬数据。
- 短包如 `TEST\r\n` 不会及时进入 RingBuffer。

处理：

- 改为轮询 DMA 写指针，增量搬运数据。
- `BspUart_ReadByte()` 和 `BspUart_Available()` 会触发 RX 轮询搬运。
- UART 测试改为双向桥接模式。

## 7. 后续上层逻辑建议

### 7.1 循迹控制

建议使用 `TrackerState.error` 作为转向误差输入：

```c
Tracker_Task10ms();
TrackerState tracker = Tracker_GetState();

if (!tracker.line_detected) {
    Chassis_Stop();
} else {
    // error -> steer/speed control
}
```

建议优先控制舵机转角，再根据误差适当调节左右电机速度。

### 7.2 障碍处理

建议在 10ms 或 20ms 模式任务中读取超声波：

```c
if (Ultrasonic_IsObstacleNear(300)) {
    Chassis_Stop();
    BspBuzzer_Play(BUZZER_PATTERN_OBSTACLE);
}
```

注意：超声波 `valid=false` 时不要直接当作近障碍，需要根据业务决定是忽略、减速还是进入保护。

### 7.3 通信接入

目前 `BtLink` / `K230Link` 解析器存在，但正常通信任务还需要把 UART 字节喂进去：

```c
uint8_t b;
while (BspUart_ReadByte(BSP_UART_K230, &b)) {
    K230Link_OnRxByte(b);
}

while (BspUart_ReadByte(BSP_UART_BT, &b)) {
    BtLink_OnRxByte(b);
}
```

这是后续进入正常 App 模式时需要优先补齐的逻辑。

### 7.4 底盘接口下一步

当前 `Chassis_SetCommand(speed, steer)` 内部仍然把左右电机设为同一速度，舵机用 `steer` 控制。后续可扩展：

- `speed_permille`：主速度
- `steer_permille`：舵机转角
- 根据 `steer_permille` 对左右电机做轻微差速补偿

建议不要在 BSP 层做车辆控制策略；BSP 只负责真实硬件输出。

## 8. 进入正常模式前检查清单

1. 将 `App/Inc/app_test.h` 中：

```c
#define TEST_SELECT  TEST_NONE
```

2. 确认 `App_Init()` 初始化路径包含：

```c
BspLed_Init();
BspBuzzer_Init();
BspKey_Init();
BspMotor_Init();
BspEncoder_Init();
BspServo_Init();
Tracker_Init();
Ultrasonic_Init();
BspUart_Init(BSP_UART_K230);
BspUart_Init(BSP_UART_BT);
Scheduler_Init();
AppTasks_Register();
```

3. 补齐通信任务中 UART 字节喂给协议解析器。

4. 上层任务避免阻塞，所有控制逻辑按周期状态机实现。

5. 保留 `tools/read_option_bytes.sh`，用于确认 PB8/BOOT0 配置未被改回。
